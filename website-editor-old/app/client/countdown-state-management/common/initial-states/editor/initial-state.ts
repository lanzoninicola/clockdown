import { EditorStateData } from "../../types/editor";

const EDITOR_INITIAL_STATE: EditorStateData = {
  currentCountdown: null,
  currentToken: "lg",
  fontSizeUnit: "px",
};

export default EDITOR_INITIAL_STATE;
