{
  "name": "clockdown",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/lanzoninicola/clockdown.git",
  "author": "Nicola <lanzoni.nicola@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev:widget:build": "cd widget && npm run build",
    "dev:widget:pub": "copyfiles -f ./widget/public/* ./website-editor/public/widget",
    "dev:widget": "npm run dev:widget:build && npm run dev:widget:pub",
    "dev:docker:up": "npm run dev:widget && docker compose -f docker-compose.yml up -d --build",
    "dev:docker:up:no-cache": "npm run dev:widget && docker compose -f docker-compose.yml up -d --build --no-cache",
    "dev:stop": "docker compose -f docker-compose.yml down"
  }
}
